package br.com.tetoproobra.compartilhado.dominio;

/**
 * Espelha as realm roles configuradas no Keycloak (ver realm-tetopro-obra.json).
 */
public enum Papel {
    ADMIN,
    GESTOR,
    INVESTIDOR_VISUALIZADOR
}
