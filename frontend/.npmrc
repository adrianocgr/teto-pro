# Força o registro público do npm, independente de qualquer configuração
# global do ambiente onde o build roda (ex.: um .npmrc de outro projeto na
# mesma VPS apontando pra um Nexus privado) — resolve builds falhando com
# "npm error code E401 ... Sonatype Nexus Repository Manager".
registry=https://registry.npmjs.org/
